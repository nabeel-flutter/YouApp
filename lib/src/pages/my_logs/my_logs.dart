import 'package:flutter/material.dart';

class MyLogs extends StatelessWidget {
  const MyLogs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('There is no appointment available yet'),
    );
  }
}